# Core pipeline (tested with Python 3.12)
numpy>=2.0
pandas>=2.2
pycalphad>=0.11
scikit-learn>=1.5
xgboost>=2.0
scipy>=1.14
torch>=2.4

# Figures and tables for the paper
matplotlib>=3.9

# Optional: only for the legacy GATv2 comparison (train.py), not needed for
# the manuscript pipeline:
# torch_geometric>=2.5

# Optional: only for regenerating the .docx manuscript (paper/make_docx.py):
# python-docx>=1.1
